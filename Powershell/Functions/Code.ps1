function script:codeDirectory{cd $codeDirectory}
function script:setupDirectory{cd $setupDirectory}
function script:coreDirectory{cd $coreDirectory}
function script:openCoreSolution{devenv $coreSolutionFile}
